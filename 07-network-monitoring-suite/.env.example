# Network Monitoring Suite - config template. Copy to .env.
# Collector
PROBE_INTERVAL_SECONDS=15
PROBE_TIMEOUT_SECONDS=2
PROBE_CONCURRENCY=8
METRICS_PORT=9100

# Grafana admin (CHANGE THESE; never commit real values)
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=change-me-strong-password
