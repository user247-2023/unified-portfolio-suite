# Monitoring targets. List ONLY hosts/services you operate or are authorized to
# monitor. Each target is probed for TCP reachability + latency.
targets:
  - name: gateway
    host: 192.168.1.1
    port: 80
  - name: internal-api
    host: api.internal.example
    port: 443
