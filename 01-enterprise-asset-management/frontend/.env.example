# EAMS frontend build-time config. Copy to .env(.local).
# Vite only exposes VITE_-prefixed variables to the browser bundle.

# Base URL of the EAMS API. (The demo runs fully in-memory and ignores this;
# wiring the store to the real REST API is the production step.)
VITE_API_URL=http://localhost:4000
