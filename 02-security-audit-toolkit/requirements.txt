# Security Audit Toolkit - runtime dependencies.
# The check/report CORE is pure stdlib (and unit-tested offline); these are only
# needed for the CLI and the network-facing checks.
click>=8.1,<9       # CLI
httpx>=0.27,<1      # http-headers check (lazily imported)
